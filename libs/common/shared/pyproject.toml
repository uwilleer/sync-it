[project]
name = "shared-lib"
dynamic = ["version"]
dependencies = [
    "async-lru>=2.0.5",
    "beautifulsoup4>=4.14.3",
    "orjson>=3.11.3",
    "pydantic[email]>=2.11.10",
]

[tool.setuptools.packages.find]
where = ["."]
